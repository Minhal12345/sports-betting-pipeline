version: 2

sources:
  - name: bronze_odds
    description: Raw odds in Unity Catalog (catalog sports_betting, schema bronze).
    database: sports_betting
    schema: bronze
    tables:
      - name: odds_snapshots
        description: Bronze odds_snapshots table (one row per game / bookmaker / outcome line).
